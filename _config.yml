# _config.yml - THE ONLY Jekyll config file
title: "Genomic Research Pipeline"
description: "Cloud-Native Solution for Large-Scale Disease Risk Prediction"
theme: jekyll-theme-cayman
show_downloads: true

# Custom variables that can be used in templates
pipeline_version: "2.1.0"
repository_url: "https://github.com/yourusername/your-repo"

# Exclude these files from processing
exclude:
  - "node_modules"
  - "*.yml"  # Exclude all YAML files from Jekyll processing
  - "*.yaml"
